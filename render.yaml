services:
  - type: web
    name: talkfridge
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn oshaberi_web_app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: FLASK_ENV
        value: production

